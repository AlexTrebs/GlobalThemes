import themeProviderWrapper from "./components/themeProvider/index";
import {
  addTheme,
  setCurrentTheme,
  toggleThemeMode,
  toggleColourBlindMode,
  addComponentThemes,
} from "./actions/index";

export {
  themeProviderWrapper as themeProvider,
  addTheme,
  setCurrentTheme,
  toggleThemeMode,
  toggleColourBlindMode,
  addComponentThemes,
};
